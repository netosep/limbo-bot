

module.exports = { 

    help: {
        name: "dc",
        aliases: ["d"]
    },

    run: async (bot, message, args) => { 

        /* var target = message.guild.member(message.mentions.users.first())

        target.disconnect("zika") */

    } 
    
}