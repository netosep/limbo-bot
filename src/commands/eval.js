

module.exports = { 

    help: {
        name: "eval",
        aliases: ["js"]
    },

    run: async (bot, message, args) => {
        
/*         var input = args.join(" ");
        var output;

        console.log(message.authort)

        try {
            output = eval(input)
        } catch (err) {
            console.error(err)
        }

        return message.channel.send(`\`\`\`js\n${output}\`\`\`` )*/

    } 
    
}